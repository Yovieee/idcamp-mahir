export default class AboutPresenter {
  constructor({ view }) {
    this._view = view;
    // About page might not need a model yet, but we can add one if needed
    this._initialize();
  }

  _initialize() {
    // Logic for about page
  }
}
